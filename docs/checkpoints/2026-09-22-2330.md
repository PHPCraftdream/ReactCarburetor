# Checkpoint — 2026-09-22 23:30

## Session summary

Continued from checkpoint `2026-09-22-1426.md` (commit `d6d66d2`). That checkpoint left the
`connect()` feature and ToDo demo update implemented but uncommitted, awaiting the user's
commit/push authorization. This session's arc, in order:

**1. `connect()` + demo: committed and pushed** (`daa9000`) after the user said
"коммит, пуш, зелёный CI".

**2. A real, systemic CI investigation and fix.** Getting to "зелёный CI" required discovering
that CI had been failing on essentially every push in the *entire* multi-session history, for
reasons unrelated to code content: the root `package-lock.json` (and `lib/package-lock.json`)
were gitignored, so every CI run re-resolved dependencies fresh with no pin, letting `oxlint`
float to whatever was newest upstream. Fixed by committing both lockfiles and switching CI's
install steps to `npm ci` (`797fef8`). That fix then *exposed* four more, previously-masked
failures (the Library job's Lint step failing first had blocked every step after it from ever
running): a memory-overcommit `ENOMEM` when `--type-aware` lint forks the native binary after
tsgolint inflates the process's address space (fixed via `vm.overcommit_memory=1`); a
`tar | grep -q` SIGPIPE under `pipefail` in the packaging job; a missing aarch64 runtime
library for the arm64 packaging leg (fixed via `apt-get download` + `dpkg -x`, sidestepping
apt's cross-arch dependency resolver); and `__tests__/Plugin/packaging.test.ts` being sensitive
to the ambient `GITHUB_ACTIONS` env var changing oxlint's own auto-selected output format
(pinned `--format=agent`). All bundled into `7e5aff3`. Diagnosed via a throwaway
`debug-ci-lint-format` branch/PR (closed and deleted once done). CI on master has been fully
green ever since.

**3. Round 1 of a JavaScript correctness/performance plan** — the user pasted a summary of
`docs/js-fix-plan-2026-09-22.md` (an 11-step plan, JS-01 through JS-11, with a dependency table)
authored by a separate review pass (commit `459fcc7`, found already sitting on local master
mid-session — not something I created; author "Marat K", the human user's own git identity) and
asked to implement it via `/babygoal` + `/wrush`. All 11 steps were implemented, each via an
isolated `/wrush` worktree, each independently zero-trust-reviewed (real diff read line-by-line,
full build/typecheck/lint/test rerun, and a revert-proof — checking out the pre-fix version of
just the changed source file and confirming the new tests actually fail without it — before
every merge), then hand-merged into the primary checkout, committed, and pushed. Where file
overlap allowed, independent steps ran as true parallel `/wrush` batches (the user explicitly
asked "можем ещё какие-то задачи параллельно решать?" mid-session and the answer was yes for
disjoint-file cases). Final commit order: `df24715`(JS-01) → `962551d`(JS-05) → `06cd77f`(JS-08)
→ `f85d129`(JS-02) → `d861ad0`(JS-03) → `50e039c`(JS-04) → `e07fff9`(JS-06) → `3703bcd`(JS-09) →
`b8eca49`(JS-07) → `6f6ad5b`(JS-10) → `45cccf8`(JS-11, the closing verification/consolidation
task). Two real bugs were found and fixed personally during zero-trust review, not re-delegated:
(a) JS-08's `moveToStablePlace` placed a freshly-`done` todo at the *end* of the done block via
`order.push(id)`, which only agrees with a true full stable resort when at most one item is
already done — fixed to share the same "insert at the boundary" logic both directions already
needed, confirmed via a hand-built reference re-implementation of the original always-resort
algorithm; (b) JS-11's sub-agent itself caught (via `npm run check:layout`, which JS-07's own
commit never ran) that JS-07 had put three exports in one file, violating this repo's
one-export-per-file convention — fixed by extracting a `Models.ts`. One deliberately-accepted,
documented limitation: JS-07's `scope.invalidations` ledger has no pruning and grows by one
entry per distinct written path for a long-lived object — traded the original bug (leaked data
objects) for a much smaller one (leaked path strings + numbers), left as lower-priority since a
fully-correct fix needs cache-lifecycle tracking this codebase's proxies don't have. React 18
compatibility was honestly reported as *not* locally exercised (only 19.3.0 was).

**4. A second review round is now in progress.** The user pasted a summary of a NEW report,
`docs/js-review-round-2-2026-09-22.md` (commit `36e9b23`, report-only, reviewing revision
`45cccf8`) — 14 findings (4 P1, 8 P2, 2 P3) confirming round 1's fixes hold for the cases they
covered, but finding correctness gaps *outside* the existing test matrix: a mid-wave computed
read that silently eats the first real notification (R2-01); unequal-depth dependency graphs
publishing an intermediate mixed-generation value before settling correctly (R2-02); `restore()`
on a `ResourceCarburetor` losing the settled answer's argument-key identity, so a restored
snapshot's data can be served for the WRONG key (R2-03); `getDerivedStateFromProps`/
`getSnapshotBeforeUpdate` causing React to skip `UNSAFE_componentWillMount` entirely (documented
React behavior), silently defeating the class-field-render tracking fallback so such a component
mounts with zero subscriptions (R2-04); JS-07's own invalidation ledger being confirmed unbounded
AND primitive-only reads never triggering a sweep at all (R2-05/06, a direct, expected follow-up
to round 1's own documented limitation); `sameSelection`'s equality check ignoring key
presence/removal and symbol-keyed properties (R2-07); abandoned render attempts still firing
queued resource loads at a later, unrelated commit (R2-08); a throwing effect cleanup aborting
the rest of unmount teardown, including subscription release (R2-09); TodoCarburetor's inline
counter arithmetic never running its "first full derivation" fallback because it zero-fills
missing counters before checking whether they were ever initialized (R2-10); the write proxy's
no-op-write check conflating absent-vs-`undefined`, signed zero, and `NaN` (R2-11); nested
(non-root) read proxies still permitting `setPrototypeOf`/`preventExtensions` that mutate the
real backing object despite the outer `connect()` facade rejecting the same operations (R2-12);
plus two P3 findings — `AntiHookComponent.tsx` at ~1260 lines with connect()/connectSelection()'s
per-attempt resolver/recorder logic duplicated between them (R2-13), and test/doc precision gaps
— unexpected console warnings not failing tests, some plan completion records overstating actual
verified coverage (R2-14).

Decomposed into 6 TaskList items respecting the report's own recommended fix order AND actual
file-overlap constraints (several findings land in the same `AntiHookComponent.tsx`/
`createReadProxy.ts`, which the report's narrative order doesn't itself encode as a literal
execution-sequencing requirement, but merge safety does): **#103** (R2-01+02, Computed.ts/
UpdateWave.ts), **#104** (R2-03, ResourceCarburetor.ts), **#105** (R2-04, AntiHookComponent.tsx
render-boundary mechanism), **#106** (R2-05+06, createProxyCache.ts + createReadProxy.ts) — all
four mutually file-disjoint, launched as a true parallel `/wrush` batch, each in its own worktree,
each already `in_progress` and confirmed genuinely running (distinct live PIDs) as of this
checkpoint; **#107** (R2-07+08+09+12, bundled into one task since all four land in
`AntiHookComponent.tsx`+`createReadProxy.ts`, avoiding four separate worktree-merge rounds on the
same files), blocked by #105 and #106; **#108** (R2-13+14, the closing consolidation/verification
task — extract shared connect()/connectSelection() machinery, tighten test/doc precision), blocked
by all five other tasks. Babysit cron `2470231f` armed (fires every 15 min, off-minute schedule).
None of #103-#106 have completed yet as of this checkpoint — no round-2 commits exist on master
yet. `docs/js-review-round-2-2026-09-22.md` itself (commit `36e9b23`) is the only round-2 content
currently on master.

## Active goal

None (`/babygoal` never sets one).

## TaskList

### in_progress
- #103 R2-01+02: fix computed mid-wave notification loss and unequal-depth graph consistency
- #104 R2-03: fix restore()/hydration losing resource data/argument-key identity
- #105 R2-04: fix render-boundary lifecycle hole with getDerivedStateFromProps/getSnapshotBeforeUpdate
- #106 R2-05+06: bound the proxy-cache invalidation ledger and reclaim on primitive reads too

### pending
- #107 R2-07+08+09+12: fix selection equality, abandoned-render resource leak, unmount cleanup, nested proxy guards  (blockedBy: #105, #106)
- #108 R2-13+14: extract shared connect()/connectSelection() machinery, tighten test/doc precision  (blockedBy: #103, #104, #105, #106, #107)

### recently completed
- #92 JS-01, #93 JS-02, #94 JS-05, #95 JS-08, #96 JS-03, #97 JS-04, #98 JS-06, #99 JS-07, #100 JS-09, #101 JS-10, #102 JS-11 — the entire round-1 plan, all merged/committed/pushed, CI green.

## Decisions

- **Bundled R2-07+08+09+12 into one task** rather than four separate ones: all four land in the
  same one or two files (`AntiHookComponent.tsx`, `createReadProxy.ts`), and round 1 already
  showed bundling same-file P2 fixes into one `/wrush` pass avoids repeated worktree-merge
  overhead without sacrificing per-finding test coverage.
- **R2-13 (the AntiHookComponent.tsx split/de-duplication refactor) is the LAST task**, gated on
  every correctness fix landing first — matching the report's own recommended order (refactor
  after correctness, not before, so the extraction has a stable, fully-fixed target).
- **R2-04's fix direction is left open to the sub-agent** (deterministic render-interception vs.
  narrowing the supported render-definition contract with a loud diagnostic) rather than
  prescribed, since the report itself frames it as an either/or with a stated preference for the
  fully-working option — the delegation prompt states that preference but doesn't mandate it.
- **Round 1's JS-07 invalidation-ledger limitation is treated as confirmed, not re-litigated**:
  R2-05/06 are exactly the follow-up round 1's own commit message predicted, not a new
  independent discovery — the R2-05+06 task's prompt says this explicitly so the sub-agent
  doesn't have to rediscover that context.

## Open questions

None currently outstanding. Round 2's push/commit cadence is expected to follow the same pattern
already established and exercised without objection through the entire round-1 arc: commit and
push after each task's zero-trust review passes, no separate confirmation needed per task.

## Repo state

```
 M .claude/scheduled_tasks.lock
```
(pre-existing, unrelated to this work — not part of any commit made this session)

```
36e9b23 docs: record round two JavaScript review with priorities
45cccf8 Verify the integrated result and fix the checks JS-11 found broken (JS-11)
b8eca49 Reclaim obsolete proxy-cache entries on written paths (JS-07)
3703bcd Resolve each connection's source once per render attempt (JS-09)
50e039c Establish a safe boundary for connected data reaching children (JS-04)
d861ad0 Make the connect() proxy facade obey JavaScript semantics (JS-03)
6f6ad5b Reuse unchanged DevTools snapshots when composing payloads (JS-10)
f85d129 Give render tracking an explicit render-attempt lifecycle (JS-02)
```

All of the above are pushed to `origin/master`. `git worktree list` currently shows four active
worktrees under `worktrees/`: `r2-01-02-computed-freshness`, `r2-03-resource-restore`,
`r2-04-render-boundary`, `r2-05-06-proxy-cache-ledger` — each holds one in-flight `/wrush` rush
session's uncommitted work-in-progress; none are merged yet.
