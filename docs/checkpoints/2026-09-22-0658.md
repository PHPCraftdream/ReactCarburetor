# Checkpoint — 2026-09-22 06:58

## Session summary

Continuing from the 2026-09-21-2030 checkpoint (commit `85e55a8`). That checkpoint left the entire
#56–68 diff (style rules, native linter, the JS-to-native bridge) sitting uncommitted, plus task #65
interrupted mid-scoping. This session did three things in sequence: finished #65 directly (not via
`/wrush`), committed and pushed the whole #56–68 backlog as three logical commits, then — on the
user's explicit `/babygoal решить все задачи с помощью /wrush, между задачи делай коммиты` — switched
into a sustained `/wrush`-driven loop that has closed 15 tasks so far, each one verified zero-trust
and committed+pushed individually. Three more are running in parallel right now.

**#65 (native `--fix`/`--fix-dry-run`), done directly.** Designed and implemented the fix pipeline
myself in-session: `native/src/fix.rs` (`Fix{start,end,text}`, `apply()` resolving overlaps by
keeping whichever fix starts first, `write_atomically()` via temp-file+rename, a line-based
`diff()` for `--fix-dry-run`), `main.rs`'s `stabilize()` (re-parse/re-run/apply loop, capped at
`MAX_FIX_PASSES=10`, discards a fix that breaks parsing rather than publishing it), and the first
real fix: `no-lifecycle-class-property` rewrites `name = (params) => body` into `name(params) body`,
carrying over accessibility/`override`/`async`/generics/return-type/rest-param (verified each is
safe to carry via a throwaway probe program against the real oxc AST before trusting it), bailing
only on a decorator, an explicit property type, `readonly`/`optional`/`definite`. 172+18 native
tests, all passing, clippy clean release build. Committed as `b50d629`.

**Then: three commits for the entire #56–68 backlog**, split by task boundary since none of it had
been committed yet: `f51e05e` (style rules #56–58), `a9b7857` (native config/suppression/all-22-rules
port #62–64/66), `ef42262` (the bridge #68 — deleted the ~200 RuleTester tests, rewired
`nativeBridge.mts`). Shared doc files (CONTRIBUTING.md, CHANGELOG.md, docs/hazards.md) went with the
last of the three commits they were relevant to, since splitting their hunks precisely wasn't worth
the risk. Full four-command verification (build/typecheck/lint/test) run before every commit.

**Then `/babygoal` kicked off the `/wrush` loop.** Domain already understood (whole session in this
repo), babysit cron already armed from earlier (`420c34c3`, 15 min), TaskList already fully
decomposed (#67, #69–90 existed from a prior session's `/task` calls) — so no re-decomposition
needed, straight to picking tasks and delegating.

**#67 (ship native linter as npm packages) — asked the user first.** The task itself flagged an
open decision ("what happens on a platform with no prebuilt binary") as belonging to the user;
asked via `AskUserQuestion` before touching anything. User picked: binary-only, loud honest failure,
no JS fallback restored. Delegated via `/wrush`; the run hit a transient 429 rate-limit after ~35
min of real progress (confirmed via `rush sessions why` — "Rate limit reached for requests", not a
hard quota/suspension) and was resumed into the same `--session` id per rush.md's own guidance
rather than escalated or retried from scratch. Finished: `npm/carburetor-lint*` packages (launcher +
6 platform packages, oxlint's own pattern), `platformPackage.mts` shared by both the standalone
launcher and the JS bridge, a CI `native-packages` matrix (6 targets, cross-compiling musl/arm64,
verified `macos-15-intel` is a real current runner label via the `actions/runner-images` API before
trusting it), and — found while wiring it up, not asked for — a real silent-failure bug in
`nativeBridge.mts`'s worker-wait path (a runner that failed left waiters reporting an empty
diagnostics array after their timeout; now the runner writes an error marker waiters re-throw).
Zero-trust review caught two small gaps the sub-agent's own summary didn't flag: the standalone
launcher advertised `CARBURETOR_LINT_BIN` in its error message without implementing it, and
`.gitignore` covered the Unix binary name but not `carburetor-lint.exe`; fixed both myself
(genuine one-liners) and verified live (override wins, invalid override falls through, no binary
fails loud with exit 2) before merging. Committed as `d71a5c8`.

**#69 (benchmark useCarburetor's proxy allocation) — delegated, verified, merged.** Real numbers:
proxy creation ~0.1 µs, shape-independent; a typical render pays well under 1 µs total; it only
shows up at dozens of reads per render or several levels of nesting. Re-ran the benchmark myself,
numbers reproduced within noise. Committed as `e3019fa`.

**#71 (audit for the same always-redo pattern elsewhere) — delegated, verified, merged.** Found
`ResourceCache.getEntry` (fresh view object + double key-encoding per render, and the fresh identity
defeats a child's `shallowEqual` prop gate — ranked #1, same frequency class as the proxy
allocation but with a real correctness angle), `connectDevTools`'s full-state re-clone per write
(every connected carburetor cloned on any single write, no-op without the extension attached), and
a minor transaction write-set double-copy. Spot-verified three of the citations against current
source (exact line numbers and quoted code all matched) before trusting the rest. Committed as
`fb5acd7`.

**#74 (StrictMode double-mount losing subscriptions) — delegated, verified, merged.** Confirmed
mechanism myself first (React's dev-only double-invoke replays mount/unmount/mount with no second
render; `releaseSubscriptions` used to clear `this.tracked` entirely, so the replayed commit had
nothing left to resubscribe). The delegated fix: `releaseSubscriptions` keeps the tracked records,
re-stamping `generation` to the current `renderGeneration` so the staleness check that normally
prunes an unread path doesn't also discard a record that only lost its live subscription — one
field, two lines, no other method touched. Verified via `git stash`: 4 of 5 new tests fail against
the unstashed... unfixed code, confirming they're real detectors, not vacuous. 266/266 full suite
after restoring. Committed as `54b234c`.

**#75 (Computed staleness: unobserved reads + diamond dependency) — delegated, verified, merged,
the most involved review of the session.** Traced both bugs myself before delegating (an
unconditional `valid=true` on unobserved recompute; eager per-dependency notification letting an
outer computed read one fresh and one still-stale input). The delegated fix went further than my own
trace predicted and was right to: `isStale()`/`hasDrifted()` compare actual recorded store versions
(flattened through nested computeds) instead of trusting `valid`, and a new `UpdateWave`
(`Store/Scheduling/UpdateWave.ts` + `...Instance.ts`, this repo's singleton-file convention) scopes
one notification pass — a computed invalidated mid-wave defers its own settle (keyed by its own id,
so repeat invalidations coalesce) instead of recomputing immediately, and the wave drains
breadth-first until the cascade stops, so a transaction touching two stores also settles a shared
dependent once instead of twice. The sub-agent's own zero-trust re-run reported "258/270 passing,
12 failures" as pre-existing/environmental; I did not accept that on its word — ran it myself, found
the worktree's `node_modules` was a 4 MB partial reconstruction (no real `npm install`, a manually
junctioned `react`) rather than genuinely broken by the diff, ran a real `npm install` +
`cargo build --release`, and got 270/270 clean. Confirmed the diamond bug's reproduction via
`git stash`: pre-fix code delivered `[20]` and *stayed stuck* — worse than my own hand-derived trace
predicted (which expected a self-correcting `[20, 21]`) — because the original recompute's own
dependency-resubscription consumed the correcting invalidation. Committed as `35fbbcc`.

**Along the way: #72/#73 deferred, not dropped.** #72 ("design a cross-render proxy cache") was
explicitly conditional on the #69 benchmark showing the proxy allocation was material — it isn't.
Rather than silently skip or silently proceed, asked the user; answer was "defer both, file a task,
revisit everything at the end." Filed **#91** (new, high id so it naturally sorts last under the
lowest-ready-id picking rule) covering both the #72/#73 relevance question and the #71 audit's actual
higher-priority finding (`ResourceCache.getEntry`), and set #72/#73's `blockedBy` to #91 so neither
gets auto-picked before that final pass runs.

**Currently in flight, three parallel `/wrush` sessions, launched together since their file sets are
disjoint** (verified before launching, not assumed):
- **#70** (skip resubscribing when the read set is unchanged across commits) — `AntiHookComponent
  .tsx` + `SubscriberIndex.ts`.
- **#76** (fix `useCarburetorValue`'s cache-key/read-tracking/object-selector bugs) —
  `lib/src/Interop/` only.
- **#86** (native lint rule gaps: missing `useResource`/`getEntry` recognition, class-scoped
  publish-exemption collision) — `native/src/rules/writes/` only; both bugs re-verified against
  current Rust source before delegating.

All three confirmed alive via `rush sessions locks` on the last several babysit ticks. None have
completed yet as of this checkpoint.

## Active goal

None (`/babygoal` does not set one; progress is TaskList-driven). Babysit cron `420c34c3` still
armed, 15-minute interval, session-only.

## TaskList

### in_progress

- #70 skip resubscribing when a component's read set is unchanged across commits — `/wrush`,
  running, file-disjoint from #76/#86
- #76 fix `useCarburetorValue`'s cache and dependency tracking (interop) — `/wrush`, running
- #86 fix native lint rule gaps: `useResource` recognition, class-scoped publish exemptions —
  `/wrush`, running

### pending

- #72 design a cross-render proxy cache for `useCarburetor` reads (blockedBy: #91) — deferred per
  user decision
- #73 implement the cross-render proxy cache and fix the audit findings (blockedBy: #72, #91) —
  deferred per user decision
- #77 fix read-tracking gaps for presence checks, aliasing, accessors, unescaped path keys
- #78 close the read-only proxy's mutation escape hatches
- #79 stop the automatic retry loop after a failed resource cache refresh
- #80 fix single-slot Suspense resource returning the wrong argument's answer
- #81 use a stable explicit key for hydration tokens instead of the shared instance counter
- #82 enforce resource cache bounds after settlement, stop leaking lastUsed metadata
- #83 define exception and direct-mutation semantics for store writes
- #84 isolate subscriber and scheduler failures so one throw can't abort delivery
- #85 normalize synchronous loader exceptions in both resource implementations
- #87 fix promise-cache.md documentation drift against the shipped ResourceCache API
- #88 extend require-tsdoc to require @param descriptions and field docs
- #89 sweep lib/src and plugin/{src,internal} for the extended TSDoc requirements (blockedBy: #88)
- #90 enable requireParamDocs/requireFieldDocs in .oxlintrc.json (blockedBy: #89)
- #91 final performance pass: revisit deferred proxy-cache design, act on audit findings — do LAST,
  by design (highest id, no other blockedBy)

### recently completed (this session, in order)

- #65 native linter: `--fix`/`--fix-dry-run` (done directly, not via `/wrush`)
- #56–58, #62–64, #66, #68 committed as the backlog from the prior session (three commits)
- #67 ship native linter as npm packages
- #69 benchmark useCarburetor's proxy allocation
- #71 audit for the always-redo pattern elsewhere
- #74 fix StrictMode double-mount losing subscriptions
- #75 fix Computed staleness (unobserved reads, diamond re-evaluation, batching)

## Decisions

- **Retroactive commit question from the last checkpoint is resolved**: the entire #56–68 backlog
  was committed (three commits) and pushed at the start of this session, before any new work began.
- **#67's platform-fallback behavior**: binary-only, loud failure naming the missing platform
  package, never a silent "zero problems"; the deleted JS rule implementations are not restored as
  a fallback path. User's explicit call, asked via `AskUserQuestion`.
- **#72/#73 deferred to the very end**, folded into new task #91 alongside the #71 audit's actual
  higher-priority finding (`ResourceCache.getEntry`) rather than pursued now, since the #69
  benchmark undercut #72's own stated precondition. User's explicit call.
- **A rush run hitting a transient 429 gets resumed into the same `--session`, not retried fresh or
  escalated to a fallback agent** — per rush.md's own documented distinction between a hard
  quota/suspension and a situational rate limit.
- **Every `/wrush` result is verified zero-trust before merging**, regardless of how thorough the
  sub-agent's own self-review reads: re-run the actual test suite (twice this session, the "green"
  claim was contradicted by an incomplete worktree environment, not the diff — fixed the environment
  and re-verified rather than accepting either "trust it" or "assume the diff is broken"), spot-check
  cited line numbers and quoted code against the real file, independently verify externally-checkable
  factual claims (the `macos-15-intel` runner label, via the `actions/runner-images` API), and
  reproduce a claimed bug via `git stash` before trusting that a new test is a real detector.
- **Small, mechanical gaps found during review get fixed directly rather than re-delegated** (the
  `CARBURETOR_LINT_BIN`/`.gitignore` gaps in #67) — re-delegation is for anything requiring judgment
  or non-trivial rework.

## Open questions

- None blocking. The only standing open item is #91 itself, which is intentionally deferred by
  design, not stalled.

## Repo state

```
(clean — nothing uncommitted in the primary checkout; three /wrush worktrees exist under
worktrees/ with their own in-progress, uncommitted diffs: skip-unchanged-resubscribe,
fix-interop-usecarburetorvalue, fix-native-rule-gaps)
```

```
35fbbcc Fix Computed staleness: unobserved reads, diamond re-evaluation, batching
54b234c Fix StrictMode's replayed mount losing every subscription
d71a5c8 Ship the native linter as platform npm packages
fb5acd7 Audit: other per-render/per-commit always-redo paths
e3019fa Benchmark useCarburetor's per-render proxy allocation cost
b50d629 Native linter: --fix and --fix-dry-run
ef42262 Bridge: JS plugin calls the native binary once per lint run
a9b7857 Native linter: config, suppression, and all 22 rules ported to Rust
f51e05e Internal style rules: max-line-length, require-tsdoc, no-blank-line-after-tsdoc
85e55a8 checkpoint: 2026-09-21-2030
```

All ten of the above are pushed to `origin/master`.
