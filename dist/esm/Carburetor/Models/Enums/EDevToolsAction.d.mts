/** Time travel actions the DevTools panel dispatches back to the app. */
export declare enum EDevToolsAction {
    JumpToAction = "JUMP_TO_ACTION",
    JumpToState = "JUMP_TO_STATE",
    Rollback = "ROLLBACK"
}
